# Run output desired format

Data for a run should be saved in the `artifacts-run-all-tensorflow` directory, with the following folder naming convention:
```
artifacts-run-all-tensorflow/<modelname>_bs<batchsize>_ep<epochs>_pat<patience>_lr<learningrate>/*
```